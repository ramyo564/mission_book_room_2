spring:
  application:
    name: dayone-reservation

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/book_table?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: Mysql1234@@!!

  jpa:
    hibernate:
      ddl-auto: create
      use-new-id-generator-mappings: false
    show-sql: true
    defer-datasource-initialization: true

  jwt:
    secret: tempSecretKey